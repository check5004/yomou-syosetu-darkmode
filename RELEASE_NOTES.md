# v1.1.0

ページ表示の配色を調整し、読書中の見やすさを改善しました。

## 変更内容

- 一部ページの背景・文字・ナビゲーションの配色を調整しました。
- 本文末尾の注意欄をダークモードで読みやすくしました。
- 対応範囲に関する古い説明を整理しました。

## インストール・更新

1. 下のAssetsから `yomou-darkmode-1.1.0.zip` をダウンロードし、任意のフォルダーに展開します。
2. Chromeで `chrome://extensions` を開き、「デベロッパーモード」をオンにします。
3. 「パッケージ化されていない拡張機能を読み込む」から、展開した `manifest.json` のあるフォルダーを選びます。
4. 開いている対象サイトのページを再読み込みします。

更新する場合はChromeを閉じ、導入済みフォルダー内のファイルを新版で置き換えてください。Chromeを開き直し、`chrome://extensions` で拡張機能を再読み込みしてから対象ページも再読み込みします。同じフォルダーを使うと設定を引き継げます。

`Source code (zip)` ではなく、`yomou-darkmode-1.1.0.zip` を使用してください。ファイルの検証用にSHA-256チェックサムも添付しています。

別ドメインの広告iframeや画像に含まれる白い背景は変更しません。Chromeウェブストアでの配布・自動更新には対応していません。

株式会社ヒナプロジェクトおよび「小説家になろう」「小説を読もう！」の公式製品ではありません。
